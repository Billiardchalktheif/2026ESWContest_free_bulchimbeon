# 네이티브 앱 — 오늘의 개발 기록 (앱 틀 + QR/PDF 완성)

> Flutter 개발환경 세팅부터 4개 화면 뼈대 완성, 그리고 서버 연동 없이 가능한 QR스캔·PDF리포트 기능까지 완료한 기록입니다. 디버깅 과정 자체가 실제 장애요인·해결방안 사례라, 개발완료보고서 4번 항목 소재로도 활용 가능합니다.

---

## 오늘 완성한 것

- [x] Flutter SDK 설치 + PATH 등록
- [x] Android Studio + SDK + 라이선스 동의
- [x] `flutter doctor` — No issues found
- [x] Flutter 프로젝트 생성 (`bulchimbeon_app`)
- [x] 실물 안드로이드 폰(갤럭시 울트라, USB 디버깅) 연동 확인
- [x] 화면 4개 뼈대 완성 — 홈 / 알림 / QR스캔 / 설정
- [x] 하단 탭바로 화면 전환 정상 작동 확인
- [x] 홈·알림 화면에 카드 UI(가짜 데이터) 적용
- [x] QR코드 스캔 기능 (mobile_scanner) — 실물 카메라로 텍스트 읽기 정상 확인
- [x] PDF 리포트 생성 (pdf/printing) — 미리보기 정상 확인 (서버 없이 가능한 최대치 완료)

---

## 오늘 겪은 장애요인과 해결 과정 (기록용)

### 장애요인 ①: 폰에 새 버전이 설치되지 않고 예전 화면만 계속 표시됨

**증상**: `main.dart`를 여러 번 수정해도(화면 4개 추가, 색상 변경 등) 폰 화면이 계속 "홈 화면 문구" 하나만 뜨고 바뀌지 않음. 코드를 아무리 고쳐도 반영이 안 되는 것처럼 보였음.

**진단 과정**:
1. 코드 자체를 의심 → `home_screen.dart` 등 개별 파일 검토, 문제없음 확인
2. **Windows 데스크톱으로 같은 코드를 실행**해서 코드 자체가 정상 작동하는지 교차 검증 → 파란 배경 테스트 화면이 정상적으로 뜸 → **코드는 처음부터 정상이었음이 확인됨**
3. 폰으로 다시 `flutter run` 실행 시 로그를 자세히 보니:
   ```
   √ Built build\app\outputs\flutter-apk\app-debug.apk   ← 빌드는 항상 성공
   Error: ADB exited with exit code 1                     ← 설치 단계에서 실패
   ```
   **빌드는 매번 성공했지만, 폰에 설치하는 단계(adb install)가 계속 실패**하고 있었음. 그래서 폰은 예전에 성공적으로 깔려있던 옛날 버전을 계속 실행 중이었던 것.

**원인**: 개발 중 앱을 여러 번 지웠다 새로 설치하는 과정에서, 폰에 남아있던 앱의 서명(디지털 인증) 정보와 새로 설치하려는 apk의 서명이 충돌한 것으로 추정.

**해결방안**: `adb uninstall com.example.bulchimbeon_app`으로 완전히 삭제 후 재설치.

**교훈**: 화면이 안 바뀌는 문제가 생기면, 코드보다 먼저 **"진짜 새 버전이 설치되고 있는가"**부터 확인하는 게 효율적임. `flutter run` 로그에서 `Error: ADB exited...` 같은 문구가 있었는지 위로 스크롤해서 확인하는 습관이 필요. Windows 데스크톱으로 먼저 테스트하는 것도 코드 문제와 설치 문제를 빠르게 구분하는 좋은 방법.

### 장애요인 ②: 앱은 정상 작동했으나 UI가 안 보이는 것처럼 보임 (색상 대비 문제)

**증상**: 위 문제 해결 후에도 하단 탭바가 화면에 안 보였음. 코드·설치 모두 정상인데 눈으로는 확인이 안 되는 상황.

**진단 과정**: 탭바가 있어야 할 위치를 무작정 클릭해보니 실제로 화면이 전환됨 — **탭바 자체는 정상 작동하고 있었고, 단지 시각적으로 "안 보였을 뿐"**임이 밝혀짐.

**원인**: `ThemeData(primarySwatch: Colors.deepOrange)` 설정이 최신 Material 버전에서 `BottomNavigationBar`의 배경색과 미선택 아이템 색을 서로 비슷한 밝은 톤으로 자동 배정 — 흰 배경에 옅은 흰 글씨처럼 되어 육안으로 구분이 안 됨.

**해결방안**: `BottomNavigationBar`에 `backgroundColor`, `selectedItemColor`, `unselectedItemColor`를 명시적으로 지정.

**교훈**: "화면에 아무것도 안 보인다"고 해서 반드시 "작동을 안 한다"는 뜻은 아님. 있어야 할 자리를 직접 클릭/터치해보는 것도 빠른 진단 방법 중 하나.

---

## 최종 코드 (5개 파일)

### `lib/main.dart`

```dart
import 'package:flutter/material.dart';
import 'home_screen.dart';
import 'alerts_screen.dart';
import 'scanner_screen.dart';
import 'settings_screen.dart';

void main() {
  runApp(const BulchimbeonApp());
}

class BulchimbeonApp extends StatelessWidget {
  const BulchimbeonApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: '불침번',
      theme: ThemeData(
        primarySwatch: Colors.deepOrange,
        useMaterial3: true,
      ),
      home: const MainNavigation(),
    );
  }
}

class MainNavigation extends StatefulWidget {
  const MainNavigation({super.key});

  @override
  State<MainNavigation> createState() => _MainNavigationState();
}

class _MainNavigationState extends State<MainNavigation> {
  int _selectedIndex = 0;

  final List<Widget> _screens = const [
    HomeScreen(),
    AlertsScreen(),
    ScannerScreen(),
    SettingsScreen(),
  ];

  final List<String> _titles = const ['불침번', '알림', 'QR 스캔', '설정'];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(_titles[_selectedIndex]),
        backgroundColor: Colors.deepOrange,
        foregroundColor: Colors.white,
      ),
      body: _screens[_selectedIndex],
      bottomNavigationBar: BottomNavigationBar(
        currentIndex: _selectedIndex,
        onTap: (index) {
          setState(() {
            _selectedIndex = index;
          });
        },
        type: BottomNavigationBarType.fixed,
        backgroundColor: Colors.white,
        selectedItemColor: Colors.deepOrange,
        unselectedItemColor: Colors.grey,
        items: const [
          BottomNavigationBarItem(icon: Icon(Icons.home), label: '홈'),
          BottomNavigationBarItem(icon: Icon(Icons.notifications), label: '알림'),
          BottomNavigationBarItem(icon: Icon(Icons.qr_code_scanner), label: 'QR'),
          BottomNavigationBarItem(icon: Icon(Icons.settings), label: '설정'),
        ],
      ),
    );
  }
}
```

### `lib/home_screen.dart`

```dart
import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  // 나중에 /api/status 연동되면 이 가짜 데이터 대신 실제 값이 들어감
  final List<Map<String, String>> _modules = const [
    {'name': '자탐1 (차동식)', 'status': 'normal'},
    {'name': '자탐2 (광전식)', 'status': 'normal'},
    {'name': '수계', 'status': 'warning'},
    {'name': '가스계', 'status': 'normal'},
    {'name': '소화기', 'status': 'normal'},
    {'name': '유도등', 'status': 'critical'},
  ];

  Color _statusColor(String status) {
    switch (status) {
      case 'normal':
        return Colors.green;
      case 'warning':
        return Colors.orange;
      case 'critical':
        return Colors.red;
      default:
        return Colors.grey;
    }
  }

  String _statusLabel(String status) {
    switch (status) {
      case 'normal':
        return '정상';
      case 'warning':
        return '주의';
      case 'critical':
        return '경보';
      default:
        return '알 수 없음';
    }
  }

  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      padding: const EdgeInsets.all(12),
      itemCount: _modules.length,
      itemBuilder: (context, index) {
        final module = _modules[index];
        final status = module['status']!;
        return Card(
          margin: const EdgeInsets.symmetric(vertical: 6),
          child: ListTile(
            leading: CircleAvatar(
              backgroundColor: _statusColor(status),
              child: const Icon(Icons.circle, color: Colors.transparent),
            ),
            title: Text(module['name']!),
            trailing: Text(
              _statusLabel(status),
              style: TextStyle(
                color: _statusColor(status),
                fontWeight: FontWeight.bold,
              ),
            ),
          ),
        );
      },
    );
  }
}
```

### `lib/alerts_screen.dart`

```dart
import 'package:flutter/material.dart';

class AlertsScreen extends StatelessWidget {
  const AlertsScreen({super.key});

  // 나중에 /api/events 연동되면 이 가짜 데이터 대신 실제 값이 들어감
  final List<Map<String, String>> _alerts = const [
    {'module': '유도등', 'message': '배터리 전압 저하 감지', 'time': '10분 전'},
    {'module': '수계', 'message': '충압펌프 기동빈도 증가', 'time': '1시간 전'},
  ];

  @override
  Widget build(BuildContext context) {
    if (_alerts.isEmpty) {
      return const Center(child: Text('현재 경보 없음'));
    }
    return ListView.builder(
      padding: const EdgeInsets.all(12),
      itemCount: _alerts.length,
      itemBuilder: (context, index) {
        final alert = _alerts[index];
        return Card(
          margin: const EdgeInsets.symmetric(vertical: 6),
          child: ListTile(
            leading: const Icon(Icons.warning_amber, color: Colors.orange),
            title: Text('${alert['module']} — ${alert['message']}'),
            subtitle: Text(alert['time']!),
            trailing: TextButton(
              onPressed: () {
                // 나중에 /api/alert/ack 연동 예정
                ScaffoldMessenger.of(context).showSnackBar(
                  const SnackBar(content: Text('확인 처리됨 (임시)')),
                );
              },
              child: const Text('확인'),
            ),
          ),
        );
      },
    );
  }
}
```

### `lib/scanner_screen.dart` (QR 스캔 기능 적용)

```dart
import 'package:flutter/material.dart';
import 'package:mobile_scanner/mobile_scanner.dart';

class ScannerScreen extends StatefulWidget {
  const ScannerScreen({super.key});

  @override
  State<ScannerScreen> createState() => _ScannerScreenState();
}

class _ScannerScreenState extends State<ScannerScreen> {
  String? _scannedCode;

  @override
  Widget build(BuildContext context) {
    if (_scannedCode != null) {
      return Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Icon(Icons.check_circle, size: 60, color: Colors.green),
            const SizedBox(height: 16),
            Text('스캔된 코드: $_scannedCode', style: const TextStyle(fontSize: 18)),
            const SizedBox(height: 8),
            const Text(
              '(나중에 서버 연동되면 이 코드로 설비 상세정보를 조회합니다)',
              style: TextStyle(color: Colors.grey, fontSize: 12),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 24),
            ElevatedButton(
              onPressed: () {
                setState(() {
                  _scannedCode = null;
                });
              },
              child: const Text('다시 스캔하기'),
            ),
          ],
        ),
      );
    }

    return MobileScanner(
      onDetect: (capture) {
        final List<Barcode> barcodes = capture.barcodes;
        if (barcodes.isNotEmpty && barcodes.first.rawValue != null) {
          setState(() {
            _scannedCode = barcodes.first.rawValue;
          });
        }
      },
    );
  }
}
```

**필요 패키지**: `flutter pub add mobile_scanner`
**추가 설정**: `android/app/src/main/AndroidManifest.xml`의 `<manifest>` 태그 바로 아래에 카메라 권한 추가
```xml
<uses-permission android:name="android.permission.CAMERA" />
```

### `lib/settings_screen.dart` (PDF 리포트 생성 기능 적용)

```dart
import 'package:flutter/material.dart';
import 'package:pdf/pdf.dart';
import 'package:pdf/widgets.dart' as pw;
import 'package:printing/printing.dart';

class SettingsScreen extends StatefulWidget {
  const SettingsScreen({super.key});

  @override
  State<SettingsScreen> createState() => _SettingsScreenState();
}

class _SettingsScreenState extends State<SettingsScreen> {
  final TextEditingController _ipController = TextEditingController(text: '192.168.0.10');

  // 나중에 서버 연동되면 이 가짜 데이터 대신 실제 /api/status, /api/events 결과가 들어감
  final List<Map<String, String>> _reportData = const [
    {'설비': '자탐1 (차동식)', '상태': '정상'},
    {'설비': '자탐2 (광전식)', '상태': '정상'},
    {'설비': '수계', '상태': '주의'},
    {'설비': '가스계', '상태': '정상'},
    {'설비': '소화기', '상태': '정상'},
    {'설비': '유도등', '상태': '경보'},
  ];

  Future<void> _generatePdf() async {
    final pdf = pw.Document();

    pdf.addPage(
      pw.Page(
        build: (context) {
          return pw.Column(
            crossAxisAlignment: pw.CrossAxisAlignment.start,
            children: [
              pw.Text('불침번 — 점검이력 리포트',
                  style: pw.TextStyle(fontSize: 20, fontWeight: pw.FontWeight.bold)),
              pw.SizedBox(height: 8),
              pw.Text('생성일: ${DateTime.now().toString().split('.')[0]}'),
              pw.SizedBox(height: 20),
              pw.Table.fromTextArray(
                headers: ['설비', '상태'],
                data: _reportData.map((row) => [row['설비']!, row['상태']!]).toList(),
              ),
            ],
          );
        },
      ),
    );

    await Printing.layoutPdf(
      onLayout: (PdfPageFormat format) async => pdf.save(),
    );
  }

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(16),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const Text('서버 IP 주소', style: TextStyle(fontWeight: FontWeight.bold)),
          const SizedBox(height: 8),
          TextField(
            controller: _ipController,
            decoration: const InputDecoration(
              border: OutlineInputBorder(),
              hintText: '예: 192.168.0.10',
            ),
          ),
          const SizedBox(height: 16),
          ElevatedButton(
            onPressed: () {
              ScaffoldMessenger.of(context).showSnackBar(
                SnackBar(content: Text('저장됨: ${_ipController.text}')),
              );
            },
            child: const Text('저장'),
          ),
          const SizedBox(height: 32),
          const Divider(),
          const SizedBox(height: 16),
          const Text('점검이력 리포트', style: TextStyle(fontWeight: FontWeight.bold)),
          const SizedBox(height: 8),
          ElevatedButton.icon(
            onPressed: _generatePdf,
            icon: const Icon(Icons.picture_as_pdf),
            label: const Text('PDF 생성'),
          ),
        ],
      ),
    );
  }
}
```

**필요 패키지**: `flutter pub add pdf` / `flutter pub add printing`

---

## 다음 단계

`네이티브앱_개발가이드.md`의 순서 기준으로, 서버 없이 가능한 것(QR스캔, PDF생성)은 완료. 남은 건 전부 서버(`api.py`, 포트 5001) 연동이 필요:

1. **서버 `api.py` 실행 상태 확인** (친구 노트북에서 MOCK_MODE로 실행 중인지)
2. **홈/알림 화면 실 데이터 연동**: 가짜 데이터(`_modules`, `_alerts`)를 실제 `/api/status`, `/api/events` 호출 결과로 교체
3. **QR 스캔 결과 → 서버 연동**: 스캔된 코드(예: `EXT-01`)를 `/api/equipment/<qr_id>`에 넘겨 실제 설비 정보 표시
4. **설정 화면 IP 저장 영구화**: `shared_preferences` 패키지로 앱 재시작해도 값 유지되게
5. **PDF 리포트 실데이터 연동**: `_reportData`를 `/api/history` 결과로 교체
6. **원격 성능시험 트리거**: 우선순위 낮음, 여유 되면 추가

---

## 참고 — 이 문서 활용처

- **GitHub**: `app/docs/` 폴더에 추가 권장
- **개발완료보고서 4번(장애요인과 해결방안)**: 위 "오늘 겪은 장애요인" 섹션을 슬라이드용으로 축약해서 활용 가능 — 하드웨어 쪽(CT클램프→INA219 등) 사례와 함께 "소프트웨어 개발 과정에서도 실측·검증 기반으로 문제를 해결했다"는 균형 잡힌 사례로 쓸 수 있음
